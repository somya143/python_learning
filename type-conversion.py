oldage = input("Enter your old-age : ")
newage = int(oldage) + 2  #here we have to convert oldage to number as the compiler takes it as string
print("This is new age" + " " + str(newage)) #Here newage is a number and compiler cannot cancate a number with string so we have to convert it to string
print(float(newage))  #this will convert an integer to a decimal or float value
#bool will convert to bollean value
